<!DOCTYPE html>
<html lang="en" dir="ltr">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Vocabulary Practice — Units 1 & 3</title>
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700;900&family=DM+Sans:wght@300;400;500;600&family=Noto+Naskh+Arabic:wght@400;600&display=swap" rel="stylesheet">
<style>
  :root {
    --bg: #0d0f1a;
    --surface: #141726;
    --card: #1a1e30;
    --border: #252a42;
    --accent: #6c63ff;
    --accent2: #ff6b9d;
    --accent3: #00d4aa;
    --gold: #ffd166;
    --text: #e8eaf6;
    --muted: #7b82b0;
    --correct: #00d4aa;
    --wrong: #ff6b9d;
    --radius: 16px;
    --radius-sm: 10px;
  }

  * { margin: 0; padding: 0; box-sizing: border-box; }

  body {
    font-family: 'DM Sans', sans-serif;
    background: var(--bg);
    color: var(--text);
    min-height: 100vh;
    overflow-x: hidden;
  }

  /* Background */
  body::before {
    content: '';
    position: fixed;
    inset: 0;
    background:
      radial-gradient(ellipse 60% 40% at 80% 20%, rgba(108,99,255,0.12) 0%, transparent 60%),
      radial-gradient(ellipse 50% 30% at 20% 80%, rgba(0,212,170,0.08) 0%, transparent 60%);
    pointer-events: none;
    z-index: 0;
  }

  .container {
    max-width: 960px;
    margin: 0 auto;
    padding: 0 20px;
    position: relative;
    z-index: 1;
  }

  /* Header */
  header {
    padding: 40px 0 30px;
    text-align: center;
    border-bottom: 1px solid var(--border);
    margin-bottom: 40px;
  }

  .unit-badges {
    display: flex;
    gap: 10px;
    justify-content: center;
    margin-bottom: 16px;
  }

  .badge {
    background: rgba(108,99,255,0.15);
    border: 1px solid rgba(108,99,255,0.4);
    color: #a99fff;
    padding: 4px 14px;
    border-radius: 20px;
    font-size: 12px;
    font-weight: 600;
    letter-spacing: 1px;
    text-transform: uppercase;
  }

  .badge.u3 {
    background: rgba(0,212,170,0.1);
    border-color: rgba(0,212,170,0.35);
    color: #00d4aa;
  }

  h1 {
    font-family: 'Playfair Display', serif;
    font-size: clamp(28px, 5vw, 48px);
    font-weight: 900;
    background: linear-gradient(135deg, #a99fff, #6c63ff, #00d4aa);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: 8px;
  }

  .subtitle {
    color: var(--muted);
    font-size: 15px;
  }

  /* Score bar */
  .score-bar {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 24px;
    padding: 16px 24px;
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    margin-bottom: 32px;
    flex-wrap: wrap;
  }

  .score-item {
    text-align: center;
  }

  .score-num {
    font-family: 'Playfair Display', serif;
    font-size: 28px;
    font-weight: 700;
    color: var(--gold);
    line-height: 1;
  }

  .score-label {
    font-size: 11px;
    color: var(--muted);
    text-transform: uppercase;
    letter-spacing: 1px;
    margin-top: 2px;
  }

  .score-divider {
    width: 1px;
    height: 36px;
    background: var(--border);
  }

  /* Nav */
  .exercise-nav {
    display: flex;
    gap: 8px;
    flex-wrap: wrap;
    justify-content: center;
    margin-bottom: 32px;
  }

  .nav-btn {
    padding: 8px 18px;
    border-radius: 30px;
    border: 1px solid var(--border);
    background: var(--surface);
    color: var(--muted);
    font-size: 13px;
    font-family: 'DM Sans', sans-serif;
    cursor: pointer;
    transition: all 0.2s;
    font-weight: 500;
  }

  .nav-btn:hover, .nav-btn.active {
    background: var(--accent);
    border-color: var(--accent);
    color: #fff;
  }

  /* Sections */
  .section {
    display: none;
    animation: fadeUp 0.4s ease;
  }

  .section.active { display: block; }

  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(16px); }
    to { opacity: 1; transform: translateY(0); }
  }

  /* Section header */
  .section-title {
    font-family: 'Playfair Display', serif;
    font-size: 24px;
    font-weight: 700;
    margin-bottom: 6px;
    color: var(--text);
  }

  .section-desc {
    color: var(--muted);
    font-size: 14px;
    margin-bottom: 24px;
  }

  /* === FLASHCARDS === */
  .flashcard-wrap {
    perspective: 1200px;
    width: 100%;
    max-width: 560px;
    height: 280px;
    margin: 0 auto 24px;
    cursor: pointer;
  }

  .flashcard-inner {
    position: relative;
    width: 100%;
    height: 100%;
    transform-style: preserve-3d;
    transition: transform 0.6s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .flashcard-wrap.flipped .flashcard-inner {
    transform: rotateY(180deg);
  }

  .flashcard-front, .flashcard-back {
    position: absolute;
    inset: 0;
    backface-visibility: hidden;
    -webkit-backface-visibility: hidden;
    border-radius: var(--radius);
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 32px;
    text-align: center;
  }

  .flashcard-front {
    background: linear-gradient(135deg, var(--card), #1f2540);
    border: 1px solid var(--border);
  }

  .flashcard-back {
    background: linear-gradient(135deg, rgba(108,99,255,0.2), rgba(0,212,170,0.15));
    border: 1px solid rgba(108,99,255,0.4);
    transform: rotateY(180deg);
  }

  .card-word {
    font-family: 'Playfair Display', serif;
    font-size: 36px;
    font-weight: 900;
    color: var(--text);
    margin-bottom: 8px;
  }

  .card-pos {
    font-size: 12px;
    color: var(--muted);
    text-transform: uppercase;
    letter-spacing: 1px;
    background: var(--border);
    padding: 3px 10px;
    border-radius: 20px;
  }

  .card-hint {
    font-size: 13px;
    color: var(--muted);
    margin-top: 16px;
  }

  .card-meaning-en {
    font-size: 16px;
    color: var(--text);
    margin-bottom: 12px;
    line-height: 1.5;
  }

  .card-meaning-ar {
    font-family: 'Noto Naskh Arabic', serif;
    font-size: 22px;
    color: var(--gold);
    direction: rtl;
    margin-bottom: 12px;
  }

  .card-example {
    font-size: 13px;
    color: var(--muted);
    font-style: italic;
    line-height: 1.5;
  }

  .flashcard-controls {
    display: flex;
    gap: 12px;
    justify-content: center;
    align-items: center;
    margin-top: 8px;
  }

  .card-counter {
    font-size: 13px;
    color: var(--muted);
    min-width: 60px;
    text-align: center;
  }

  /* Buttons */
  .btn {
    padding: 10px 24px;
    border-radius: 30px;
    border: none;
    font-family: 'DM Sans', sans-serif;
    font-size: 14px;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.2s;
  }

  .btn-primary {
    background: var(--accent);
    color: #fff;
  }

  .btn-primary:hover { background: #5a52e0; transform: translateY(-1px); }

  .btn-outline {
    background: transparent;
    border: 1px solid var(--border);
    color: var(--muted);
  }

  .btn-outline:hover {
    border-color: var(--accent);
    color: var(--accent);
  }

  .btn-icon {
    width: 42px;
    height: 42px;
    border-radius: 50%;
    border: 1px solid var(--border);
    background: var(--surface);
    color: var(--text);
    font-size: 18px;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.2s;
  }

  .btn-icon:hover {
    background: var(--accent);
    border-color: var(--accent);
  }

  /* === MCQ === */
  .question-card {
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 28px;
    margin-bottom: 20px;
  }

  .q-num {
    font-size: 11px;
    color: var(--muted);
    text-transform: uppercase;
    letter-spacing: 1px;
    margin-bottom: 10px;
  }

  .q-text {
    font-size: 17px;
    font-weight: 500;
    margin-bottom: 20px;
    line-height: 1.6;
  }

  .q-text .blank {
    display: inline-block;
    min-width: 90px;
    border-bottom: 2px solid var(--accent);
    color: var(--accent);
    font-weight: 600;
    text-align: center;
  }

  .options {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 10px;
  }

  @media (max-width: 500px) {
    .options { grid-template-columns: 1fr; }
  }

  .option {
    padding: 12px 16px;
    border-radius: var(--radius-sm);
    border: 1px solid var(--border);
    background: var(--surface);
    color: var(--text);
    font-family: 'DM Sans', sans-serif;
    font-size: 14px;
    cursor: pointer;
    transition: all 0.2s;
    text-align: left;
    display: flex;
    align-items: center;
    gap: 10px;
  }

  .option:hover:not(:disabled) {
    border-color: var(--accent);
    background: rgba(108,99,255,0.1);
  }

  .option:disabled { cursor: default; }

  .option.correct {
    border-color: var(--correct);
    background: rgba(0,212,170,0.12);
    color: var(--correct);
  }

  .option.wrong {
    border-color: var(--wrong);
    background: rgba(255,107,157,0.1);
    color: var(--wrong);
  }

  .opt-letter {
    width: 24px;
    height: 24px;
    border-radius: 50%;
    background: var(--border);
    font-size: 11px;
    font-weight: 700;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }

  .feedback {
    margin-top: 12px;
    padding: 10px 14px;
    border-radius: var(--radius-sm);
    font-size: 13px;
    display: none;
  }

  .feedback.show { display: block; }

  .feedback.correct-fb {
    background: rgba(0,212,170,0.1);
    border: 1px solid rgba(0,212,170,0.3);
    color: var(--correct);
  }

  .feedback.wrong-fb {
    background: rgba(255,107,157,0.1);
    border: 1px solid rgba(255,107,157,0.3);
    color: var(--wrong);
  }

  /* Arabic question */
  .q-ar {
    font-family: 'Noto Naskh Arabic', serif;
    direction: rtl;
    font-size: 18px;
  }

  /* === MATCHING === */
  .matching-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 12px;
    margin-bottom: 20px;
  }

  .match-col-title {
    font-size: 12px;
    color: var(--muted);
    text-transform: uppercase;
    letter-spacing: 1px;
    text-align: center;
    margin-bottom: 8px;
  }

  .match-item {
    padding: 12px 16px;
    border-radius: var(--radius-sm);
    border: 1px solid var(--border);
    background: var(--surface);
    cursor: pointer;
    font-size: 14px;
    transition: all 0.2s;
    text-align: center;
    min-height: 52px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .match-item:hover:not(.matched):not(.selected-match) {
    border-color: var(--accent);
  }

  .match-item.selected-match {
    border-color: var(--gold);
    background: rgba(255,209,102,0.1);
    color: var(--gold);
  }

  .match-item.matched {
    border-color: var(--correct);
    background: rgba(0,212,170,0.1);
    color: var(--correct);
    cursor: default;
  }

  .match-item.wrong-match {
    border-color: var(--wrong);
    background: rgba(255,107,157,0.1);
    animation: shake 0.4s ease;
  }

  .match-ar {
    font-family: 'Noto Naskh Arabic', serif;
    font-size: 18px;
    direction: rtl;
  }

  @keyframes shake {
    0%, 100% { transform: translateX(0); }
    25% { transform: translateX(-6px); }
    75% { transform: translateX(6px); }
  }

  /* === FILL IN THE BLANK === */
  .fill-input {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 8px;
    color: var(--text);
    font-family: 'DM Sans', sans-serif;
    font-size: 15px;
    padding: 8px 14px;
    width: 160px;
    outline: none;
    transition: border-color 0.2s;
    display: inline-block;
    text-align: center;
  }

  .fill-input:focus { border-color: var(--accent); }
  .fill-input.correct { border-color: var(--correct); color: var(--correct); }
  .fill-input.wrong { border-color: var(--wrong); color: var(--wrong); }

  .check-btn {
    margin-top: 16px;
    display: block;
  }

  /* === WORD SCRAMBLE === */
  .scramble-box {
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 28px;
    margin-bottom: 20px;
    text-align: center;
  }

  .scrambled-word {
    display: flex;
    gap: 8px;
    justify-content: center;
    flex-wrap: wrap;
    margin: 16px 0;
  }

  .letter-tile {
    width: 44px;
    height: 44px;
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 20px;
    font-weight: 700;
    font-family: 'Playfair Display', serif;
    color: var(--accent);
    cursor: pointer;
    transition: all 0.15s;
    user-select: none;
  }

  .letter-tile:hover:not(.used) { background: rgba(108,99,255,0.15); transform: translateY(-2px); }
  .letter-tile.used { opacity: 0.25; cursor: default; transform: none; }

  .answer-tiles {
    display: flex;
    gap: 8px;
    justify-content: center;
    flex-wrap: wrap;
    min-height: 52px;
    border-bottom: 2px solid var(--accent);
    padding-bottom: 8px;
    margin-bottom: 16px;
  }

  .answer-tile {
    width: 44px;
    height: 44px;
    background: rgba(108,99,255,0.15);
    border: 1px solid var(--accent);
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 20px;
    font-weight: 700;
    font-family: 'Playfair Display', serif;
    color: var(--text);
    cursor: pointer;
    transition: all 0.15s;
  }

  .answer-tile:hover { background: rgba(255,107,157,0.15); border-color: var(--wrong); }

  /* === TRUE/FALSE === */
  .tf-card {
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 28px;
    margin-bottom: 20px;
  }

  .tf-statement {
    font-size: 16px;
    line-height: 1.6;
    margin-bottom: 20px;
  }

  .tf-buttons {
    display: flex;
    gap: 12px;
  }

  .tf-btn {
    flex: 1;
    padding: 12px;
    border-radius: var(--radius-sm);
    border: 1px solid var(--border);
    background: var(--surface);
    color: var(--text);
    font-family: 'DM Sans', sans-serif;
    font-size: 15px;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.2s;
  }

  .tf-btn:hover:not(:disabled) { transform: translateY(-2px); }
  .tf-btn:disabled { cursor: default; }

  .tf-btn.true-btn:hover:not(:disabled), .tf-btn.true-btn.selected-correct {
    background: rgba(0,212,170,0.15);
    border-color: var(--correct);
    color: var(--correct);
  }

  .tf-btn.false-btn:hover:not(:disabled), .tf-btn.false-btn.selected-correct {
    background: rgba(255,107,157,0.15);
    border-color: var(--wrong);
    color: var(--wrong);
  }

  .tf-btn.selected-wrong {
    background: rgba(255,107,157,0.1);
    border-color: var(--wrong);
    color: var(--wrong);
  }

  /* Progress */
  .progress-track {
    background: var(--border);
    border-radius: 4px;
    height: 4px;
    margin-bottom: 28px;
    overflow: hidden;
  }

  .progress-fill {
    height: 100%;
    background: linear-gradient(90deg, var(--accent), var(--accent3));
    border-radius: 4px;
    transition: width 0.3s ease;
  }

  /* Glossary */
  .glossary-filter {
    display: flex;
    gap: 8px;
    flex-wrap: wrap;
    margin-bottom: 20px;
  }

  .filter-chip {
    padding: 6px 14px;
    border-radius: 20px;
    border: 1px solid var(--border);
    background: var(--surface);
    color: var(--muted);
    font-size: 12px;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.2s;
    letter-spacing: 0.5px;
  }

  .filter-chip.active, .filter-chip:hover {
    background: var(--accent);
    border-color: var(--accent);
    color: #fff;
  }

  .glossary-table {
    width: 100%;
    border-collapse: collapse;
  }

  .glossary-table th {
    text-align: left;
    font-size: 11px;
    color: var(--muted);
    text-transform: uppercase;
    letter-spacing: 1px;
    padding: 10px 14px;
    border-bottom: 1px solid var(--border);
  }

  .glossary-table td {
    padding: 14px;
    border-bottom: 1px solid rgba(37,42,66,0.5);
    font-size: 14px;
    vertical-align: middle;
  }

  .glossary-table tr:hover td { background: rgba(108,99,255,0.04); }

  .word-cell {
    font-weight: 600;
    color: var(--text);
  }

  .pos-pill {
    font-size: 10px;
    padding: 2px 8px;
    border-radius: 10px;
    background: rgba(108,99,255,0.15);
    color: #a99fff;
    font-weight: 600;
    display: inline-block;
  }

  .ar-cell {
    font-family: 'Noto Naskh Arabic', serif;
    font-size: 16px;
    direction: rtl;
    text-align: right;
    color: var(--gold);
  }

  .unit-tag {
    font-size: 10px;
    padding: 2px 6px;
    border-radius: 6px;
    font-weight: 600;
  }

  .unit-tag.u1 { background: rgba(108,99,255,0.2); color: #a99fff; }
  .unit-tag.u3 { background: rgba(0,212,170,0.15); color: var(--correct); }

  /* Results modal */
  .results-overlay {
    position: fixed;
    inset: 0;
    background: rgba(13,15,26,0.85);
    backdrop-filter: blur(8px);
    z-index: 100;
    display: flex;
    align-items: center;
    justify-content: center;
    display: none;
  }

  .results-overlay.show { display: flex; }

  .results-modal {
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: 24px;
    padding: 48px 40px;
    max-width: 420px;
    width: 90%;
    text-align: center;
    animation: popIn 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
  }

  @keyframes popIn {
    from { opacity: 0; transform: scale(0.85); }
    to { opacity: 1; transform: scale(1); }
  }

  .results-score {
    font-family: 'Playfair Display', serif;
    font-size: 72px;
    font-weight: 900;
    color: var(--gold);
    line-height: 1;
    margin: 16px 0;
  }

  .results-msg {
    color: var(--muted);
    margin-bottom: 28px;
    line-height: 1.6;
  }

  /* Word search grid */
  .ws-grid {
    display: inline-grid;
    gap: 4px;
    margin-bottom: 20px;
  }

  .ws-cell {
    width: 36px;
    height: 36px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 15px;
    font-weight: 700;
    font-family: 'Playfair Display', serif;
    color: var(--muted);
    border-radius: 6px;
    background: var(--surface);
    cursor: pointer;
    user-select: none;
    transition: all 0.1s;
  }

  .ws-cell.selected { background: rgba(108,99,255,0.3); color: var(--text); }
  .ws-cell.found { background: rgba(0,212,170,0.2); color: var(--correct); }

  .ws-words {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
  }

  .ws-word {
    font-size: 13px;
    font-weight: 600;
    padding: 4px 12px;
    border-radius: 20px;
    border: 1px solid var(--border);
    color: var(--muted);
    background: var(--surface);
    transition: all 0.2s;
  }

  .ws-word.found { border-color: var(--correct); color: var(--correct); background: rgba(0,212,170,0.1); text-decoration: line-through; }

  /* Streak */
  .streak-bar {
    display: flex;
    gap: 4px;
    justify-content: center;
    margin-bottom: 20px;
  }

  .streak-dot {
    width: 10px;
    height: 10px;
    border-radius: 50%;
    background: var(--border);
    transition: background 0.3s;
  }

  .streak-dot.correct { background: var(--correct); }
  .streak-dot.wrong { background: var(--wrong); }

  footer {
    text-align: center;
    padding: 40px 20px;
    color: var(--muted);
    font-size: 12px;
    border-top: 1px solid var(--border);
    margin-top: 60px;
  }

  .section-actions {
    display: flex;
    gap: 10px;
    align-items: center;
    margin-bottom: 20px;
    flex-wrap: wrap;
  }

  /* Responsive */
  @media (max-width: 640px) {
    .matching-grid { grid-template-columns: 1fr; }
    .flashcard-wrap { height: 240px; }
    .card-word { font-size: 28px; }
  }
</style>
</head>
<body>

<div class="container">
  <header>
    <div class="unit-badges">
      <span class="badge">Unit 1</span>
      <span class="badge u3">Unit 3</span>
    </div>
    <h1>Vocabulary Master</h1>
    <p class="subtitle">All words • English & Arabic • Multiple exercise types</p>
  </header>

  <!-- Score bar -->
  <div class="score-bar">
    <div class="score-item">
      <div class="score-num" id="totalScore">0</div>
      <div class="score-label">Total Points</div>
    </div>
    <div class="score-divider"></div>
    <div class="score-item">
      <div class="score-num" id="correctCount">0</div>
      <div class="score-label">Correct</div>
    </div>
    <div class="score-divider"></div>
    <div class="score-item">
      <div class="score-num" id="streakCount">0</div>
      <div class="score-label">Streak 🔥</div>
    </div>
    <div class="score-divider"></div>
    <div class="score-item">
      <div class="score-num" id="wordsLearned">0</div>
      <div class="score-label">Words Seen</div>
    </div>
  </div>

  <!-- Navigation -->
  <nav class="exercise-nav">
    <button class="nav-btn active" onclick="showSection('glossary')">📖 Glossary</button>
    <button class="nav-btn" onclick="showSection('flashcards')">🃏 Flashcards</button>
    <button class="nav-btn" onclick="showSection('mcq-en')">✅ MCQ (EN)</button>
    <button class="nav-btn" onclick="showSection('mcq-ar')">✅ MCQ (AR)</button>
    <button class="nav-btn" onclick="showSection('fill')">✏️ Fill Blank</button>
    <button class="nav-btn" onclick="showSection('matching')">🔗 Matching</button>
    <button class="nav-btn" onclick="showSection('truefalse')">⚡ True/False</button>
    <button class="nav-btn" onclick="showSection('scramble')">🔀 Scramble</button>
    <button class="nav-btn" onclick="showSection('wordsearch')">🔍 Word Search</button>
  </nav>

  <!-- ===================== GLOSSARY ===================== -->
  <section id="glossary" class="section active">
    <div class="section-title">📖 Word Glossary</div>
    <div class="section-desc">All vocabulary from Units 1 & 3 — tap a row to see the example sentence</div>
    <div class="glossary-filter">
      <span class="filter-chip active" onclick="filterGlossary('all', this)">All</span>
      <span class="filter-chip" onclick="filterGlossary('u1', this)">Unit 1</span>
      <span class="filter-chip" onclick="filterGlossary('u3', this)">Unit 3</span>
      <span class="filter-chip" onclick="filterGlossary('VERB', this)">Verbs</span>
      <span class="filter-chip" onclick="filterGlossary('NOUN', this)">Nouns</span>
      <span class="filter-chip" onclick="filterGlossary('ADJ', this)">Adjectives</span>
    </div>
    <table class="glossary-table" id="glossaryTable">
      <thead>
        <tr>
          <th>Word</th>
          <th>POS</th>
          <th>Meaning</th>
          <th style="text-align:right">المعنى</th>
          <th>Unit</th>
        </tr>
      </thead>
      <tbody id="glossaryBody"></tbody>
    </table>
  </section>

  <!-- ===================== FLASHCARDS ===================== -->
  <section id="flashcards" class="section">
    <div class="section-title">🃏 Flashcards</div>
    <div class="section-desc">Click the card to flip it and see the meaning in English & Arabic</div>

    <div class="progress-track">
      <div class="progress-fill" id="fcProgress" style="width:0%"></div>
    </div>

    <div class="flashcard-wrap" id="flashcard" onclick="flipCard()">
      <div class="flashcard-inner">
        <div class="flashcard-front" id="fcFront">
          <div class="card-word" id="fcWord">—</div>
          <div class="card-pos" id="fcPos">—</div>
          <div class="card-hint">Tap to reveal meaning</div>
        </div>
        <div class="flashcard-back" id="fcBack">
          <div class="card-meaning-en" id="fcMeaning">—</div>
          <div class="card-meaning-ar" id="fcArabic">—</div>
          <div class="card-example" id="fcExample">—</div>
        </div>
      </div>
    </div>

    <div class="flashcard-controls">
      <button class="btn-icon" onclick="prevCard()">←</button>
      <div class="card-counter" id="fcCounter">1 / 40</div>
      <button class="btn-icon" onclick="nextCard()">→</button>
    </div>

    <div style="text-align:center; margin-top:20px; display:flex; gap:12px; justify-content:center; flex-wrap:wrap">
      <button class="btn btn-outline" onclick="shuffleCards()">🔀 Shuffle</button>
      <button class="btn btn-outline" onclick="resetCards()">↺ Restart</button>
      <button class="btn btn-primary" onclick="showSection('mcq-en')">Start Quiz →</button>
    </div>
  </section>

  <!-- ===================== MCQ ENGLISH ===================== -->
  <section id="mcq-en" class="section">
    <div class="section-title">✅ Multiple Choice — English</div>
    <div class="section-desc">Choose the correct definition for each word</div>
    <div class="streak-bar" id="streakBarEN"></div>
    <div class="progress-track">
      <div class="progress-fill" id="mcqEnProgress" style="width:0%"></div>
    </div>
    <div id="mcqEnContainer"></div>
    <button class="btn btn-primary" onclick="generateMCQ_EN()">🔄 New Set</button>
  </section>

  <!-- ===================== MCQ ARABIC ===================== -->
  <section id="mcq-ar" class="section">
    <div class="section-title">✅ Multiple Choice — العربية</div>
    <div class="section-desc">اختر المعنى الصحيح للكلمة بالعربية</div>
    <div class="streak-bar" id="streakBarAR"></div>
    <div class="progress-track">
      <div class="progress-fill" id="mcqArProgress" style="width:0%"></div>
    </div>
    <div id="mcqArContainer"></div>
    <button class="btn btn-primary" onclick="generateMCQ_AR()">🔄 مجموعة جديدة</button>
  </section>

  <!-- ===================== FILL IN BLANK ===================== -->
  <section id="fill" class="section">
    <div class="section-title">✏️ Fill in the Blank</div>
    <div class="section-desc">Type the correct word to complete each sentence</div>
    <div class="progress-track">
      <div class="progress-fill" id="fillProgress" style="width:0%"></div>
    </div>
    <div id="fillContainer"></div>
    <div style="display:flex;gap:10px;flex-wrap:wrap">
      <button class="btn btn-primary" onclick="checkAllFill()">✓ Check Answers</button>
      <button class="btn btn-outline" onclick="generateFill()">🔄 New Set</button>
    </div>
  </section>

  <!-- ===================== MATCHING ===================== -->
  <section id="matching" class="section">
    <div class="section-title">🔗 Matching</div>
    <div class="section-desc">Click a word on the left, then its Arabic meaning on the right</div>
    <div class="progress-track">
      <div class="progress-fill" id="matchProgress" style="width:0%"></div>
    </div>
    <div class="matching-grid" id="matchContainer"></div>
    <button class="btn btn-outline" onclick="generateMatching()">🔄 New Set</button>
  </section>

  <!-- ===================== TRUE / FALSE ===================== -->
  <section id="truefalse" class="section">
    <div class="section-title">⚡ True or False</div>
    <div class="section-desc">Is the definition correct for the given word?</div>
    <div class="streak-bar" id="streakBarTF"></div>
    <div class="progress-track">
      <div class="progress-fill" id="tfProgress" style="width:0%"></div>
    </div>
    <div id="tfContainer"></div>
    <button class="btn btn-primary" onclick="generateTF()">🔄 New Set</button>
  </section>

  <!-- ===================== SCRAMBLE ===================== -->
  <section id="scramble" class="section">
    <div class="section-title">🔀 Word Scramble</div>
    <div class="section-desc">Click the letters in the right order to spell the word</div>
    <div class="progress-track">
      <div class="progress-fill" id="scrambleProgress" style="width:0%"></div>
    </div>
    <div id="scrambleContainer"></div>
    <div style="display:flex;gap:10px;flex-wrap:wrap">
      <button class="btn btn-primary" onclick="checkScramble()">✓ Check</button>
      <button class="btn btn-outline" onclick="nextScramble()">→ Next Word</button>
      <button class="btn btn-outline" onclick="generateScramble()">🔄 New Set</button>
    </div>
  </section>

  <!-- ===================== WORD SEARCH ===================== -->
  <section id="wordsearch" class="section">
    <div class="section-title">🔍 Word Search</div>
    <div class="section-desc">Click the first and last letter of a word to highlight it. Words go left→right or top→bottom.</div>
    <div id="wsContainer"></div>
    <button class="btn btn-primary" onclick="generateWordSearch()">🔄 New Puzzle</button>
  </section>
</div>

<!-- Results overlay -->
<div class="results-overlay" id="resultsOverlay">
  <div class="results-modal">
    <div style="font-size:48px" id="resultEmoji">🎉</div>
    <div class="results-score" id="resultScore">8/10</div>
    <div class="section-title" id="resultTitle">Great job!</div>
    <div class="results-msg" id="resultMsg">You're mastering these words!</div>
    <div style="display:flex;gap:10px;justify-content:center">
      <button class="btn btn-outline" onclick="closeResults()">Close</button>
      <button class="btn btn-primary" id="resultNext" onclick="closeResults()">Next Exercise →</button>
    </div>
  </div>
</div>

<footer>
  Vocabulary Master — Units 1 & 3 · Tamkeen
</footer>

<script>
// ============================================================
// DATA
// ============================================================
const words = [
  // UNIT 1
  { word: "capture", pos: "VERB", meaning: "To catch someone and make them your prisoner", arabic: "يأسر", example: "Two soldiers were captured by the enemy.", unit: "u1" },
  { word: "dissolve", pos: "VERB", meaning: "If a solid dissolves, it becomes part of a liquid", arabic: "يذوب / ينحل", example: "These tablets dissolve in water.", unit: "u1" },
  { word: "e-waste", pos: "NOUN", meaning: "Old or broken electronic products that are thrown away", arabic: "خردة الكترونية", example: "Environmentalists urge companies to reduce e-waste.", unit: "u1" },
  { word: "export", pos: "NOUN", meaning: "A product one country makes and sells to other countries", arabic: "تصدير", example: "Scottish beef exports to Japan.", unit: "u1" },
  { word: "leak", pos: "VERB", meaning: "To allow liquid or gas to come out when it should not", arabic: "يتسرب", example: "The bottle had leaked and the bag was wet.", unit: "u1" },
  { word: "obsolete", pos: "ADJ", meaning: "No longer used or needed", arabic: "مهمل / مهجور", example: "Will books become obsolete because of computers?", unit: "u1" },
  { word: "protect", pos: "VERB", meaning: "To keep someone or something safe from danger", arabic: "يحمي", example: "It's important to protect your skin from the sun.", unit: "u1" },
  { word: "recycle", pos: "VERB", meaning: "To use paper, glass, or plastic again instead of throwing it away", arabic: "يعيد تدوير", example: "We recycle all our paper and glass.", unit: "u1" },
  { word: "regain popularity", pos: "VERB", meaning: "To return to being well-liked after a period of decline", arabic: "استعادة الشعبية", example: "The band's album helped them regain popularity.", unit: "u1" },
  { word: "shift", pos: "NOUN", meaning: "A period of work in a factory or hospital", arabic: "نوبة عمل / وردية", example: "She works the night shift.", unit: "u1" },
  { word: "skyrocket", pos: "VERB", meaning: "To rise extremely quickly in price or success", arabic: "يرتفع فجأة وبسرعة", example: "Housing prices have skyrocketed in recent months.", unit: "u1" },
  { word: "virtual space storage", pos: "NOUN", meaning: "A system for storing computer data that can be used from anywhere online", arabic: "تخزين الفضاء الافتراضي", example: "Our company offers virtual storage solutions.", unit: "u1" },
  { word: "once and for all", pos: "IDIOM", meaning: "Completely and finally", arabic: "مرة واحدة وإلى الأبد", example: "We have to decide, once and for all.", unit: "u1" },
  { word: "so-called", pos: "ADJ", meaning: "Used to show that a word used to describe something is wrong", arabic: "المدعو", example: "My so-called friend stole money from me.", unit: "u1" },
  { word: "associated with", pos: "PHRASAL VERB", meaning: "To spend time with a group of people, especially disapproved ones", arabic: "مرتبط بـ", example: "I don't want my children associating with drug addicts.", unit: "u1" },
  { word: "compensate", pos: "VERB", meaning: "To pay someone money for something lost or damaged", arabic: "يكافئ", example: "Victims of the crash will be compensated for their injuries.", unit: "u1" },
  { word: "debunk", pos: "VERB", meaning: "To show that something is less true than it appears", arabic: "فضح", example: "The writer's aim was to debunk the myth.", unit: "u1" },
  { word: "intake", pos: "NOUN", meaning: "The amount of food or drink taken into the body", arabic: "استيعاب", example: "Reducing your salt intake can lower blood pressure.", unit: "u1" },
  { word: "moderate", pos: "ADJ", meaning: "Average in size or amount; not too much", arabic: "معتدل", example: "Eating a moderate amount of fat is healthy.", unit: "u1" },
  { word: "myth", pos: "NOUN", meaning: "An ancient story about gods and brave people", arabic: "أسطورة", example: "A Greek myth.", unit: "u1" },
  // UNIT 3
  { word: "commodity", pos: "NOUN", meaning: "A raw material or primary product that can be bought and sold", arabic: "سلعة", example: "Oil is a valuable commodity in global markets.", unit: "u3" },
  { word: "consumers", pos: "NOUN", meaning: "People who buy and use goods and services", arabic: "مستهلكون", example: "Consumers are becoming more conscious of their health.", unit: "u3" },
  { word: "demand", pos: "NOUN/VERB", meaning: "The desire for a good or service / to ask for something forcefully", arabic: "طلب (كاسم) / يطلب (كفعل)", example: "The demand for electric cars is increasing.", unit: "u3" },
  { word: "determine", pos: "VERB", meaning: "To decide or establish something after consideration", arabic: "يحدد", example: "We need to determine the cause of the problem.", unit: "u3" },
  { word: "equivalent", pos: "ADJ", meaning: "Equal in value, amount, or function", arabic: "مكافئ", example: "One dollar is approximately equivalent to 0.9 euros.", unit: "u3" },
  { word: "fluctuation", pos: "NOUN", meaning: "A continuous change between one level and another", arabic: "تقلب", example: "The price of gold shows constant fluctuation.", unit: "u3" },
  { word: "income", pos: "NOUN", meaning: "Money that a person or business earns regularly", arabic: "دخل", example: "His monthly income is quite high.", unit: "u3" },
  { word: "motive", pos: "NOUN", meaning: "A reason for doing something", arabic: "دافع", example: "His motive for working hard is to support his family.", unit: "u3" },
  { word: "purchase", pos: "VERB/NOUN", meaning: "The act of buying something / to buy something", arabic: "شراء (كاسم) / يشتري (كفعل)", example: "She made a big purchase today.", unit: "u3" },
  { word: "reduction", pos: "NOUN", meaning: "The act of making something smaller or less", arabic: "تخفيض", example: "The company announced a price reduction.", unit: "u3" },
  { word: "supplies", pos: "NOUN/VERB", meaning: "Goods and materials available / to provide something", arabic: "إمدادات (كاسم) / يزود (كفعل)", example: "The hospital received medical supplies.", unit: "u3" },
  { word: "charged", pos: "VERB/ADJ", meaning: "To ask for money as a price / filled with electricity or emotion", arabic: "يفرض رسوماً / مشحون", example: "He was charged $50 for the service.", unit: "u3" },
  { word: "conditions", pos: "NOUN", meaning: "The circumstances affecting the way people live or work", arabic: "ظروف", example: "The working conditions have improved over the years.", unit: "u3" },
  { word: "funding", pos: "NOUN", meaning: "Money provided for a particular purpose", arabic: "تمويل", example: "The project received government funding.", unit: "u3" },
  { word: "indicate", pos: "VERB", meaning: "To show or point out something", arabic: "يشير إلى", example: "The map indicates the nearest gas station.", unit: "u3" },
  { word: "predictions", pos: "NOUN", meaning: "Statements about what will happen in the future", arabic: "تنبؤات", example: "His predictions about the weather were accurate.", unit: "u3" },
  { word: "promising", pos: "ADJ", meaning: "Showing signs of future success", arabic: "واعد", example: "She is a promising young athlete.", unit: "u3" },
  { word: "spiked", pos: "VERB", meaning: "To increase sharply or suddenly", arabic: "ارتفع بشكل حاد", example: "The price of oil spiked last month.", unit: "u3" },
  { word: "adopt", pos: "VERB", meaning: "To take up or start using something new", arabic: "يتبنى", example: "The company decided to adopt a new marketing strategy.", unit: "u3" },
  { word: "approach", pos: "NOUN/VERB", meaning: "A way of dealing with something / to move closer", arabic: "نهج (كاسم) / يقترب (كفعل)", example: "We need a new approach to solve this problem.", unit: "u3" },
  { word: "authority", pos: "NOUN", meaning: "The power or right to give orders or make decisions", arabic: "سلطة", example: "The teacher has the authority to discipline students.", unit: "u3" },
  { word: "ensure", pos: "VERB", meaning: "To make sure or guarantee something", arabic: "يضمن", example: "Please ensure that all doors are locked.", unit: "u3" },
  { word: "equilibrium", pos: "NOUN", meaning: "A state of balance between opposing forces", arabic: "توازن", example: "The market reached a state of equilibrium.", unit: "u3" },
  { word: "intrinsic", pos: "ADJ", meaning: "Belonging naturally; essential", arabic: "جوهري", example: "Honesty is an intrinsic value in leadership.", unit: "u3" },
  { word: "latter", pos: "ADJ", meaning: "Referring to the second of two things mentioned", arabic: "الأخير", example: "I prefer the latter option.", unit: "u3" },
  { word: "maintain", pos: "VERB", meaning: "To keep something in good condition or at the same level", arabic: "يحافظ على", example: "It's important to maintain a healthy lifestyle.", unit: "u3" },
  { word: "stability", pos: "NOUN", meaning: "The state of being steady and not changing", arabic: "استقرار", example: "Political stability is crucial for economic growth.", unit: "u3" },
  { word: "accountable", pos: "ADJ", meaning: "Responsible for decisions and expected to explain them", arabic: "مسؤول", example: "Managers are accountable for their teams' performance.", unit: "u3" },
  { word: "counterfeit", pos: "ADJ", meaning: "Fake or not genuine", arabic: "مزيف", example: "The store discovered some counterfeit bills.", unit: "u3" },
  { word: "deter", pos: "VERB", meaning: "To discourage someone from doing something by making them fear consequences", arabic: "يردع", example: "High fines are meant to deter illegal activity.", unit: "u3" },
  { word: "distinct", pos: "ADJ", meaning: "Clearly different or separate", arabic: "مميز / مختلف", example: "There are distinct differences between the two products.", unit: "u3" },
  { word: "legitimacy", pos: "NOUN", meaning: "The quality of being legal or acceptable", arabic: "شرعية", example: "The court questioned the legitimacy of the contract.", unit: "u3" },
  { word: "merge with", pos: "PHRASE", meaning: "To combine with something else to form a single entity", arabic: "يندمج مع", example: "The two companies will merge with each other next year.", unit: "u3" },
  { word: "particles", pos: "NOUN", meaning: "Very small pieces or amounts of something", arabic: "جسيمات", example: "Dust particles were floating in the air.", unit: "u3" },
  { word: "resistant", pos: "ADJ", meaning: "Not affected or harmed by something", arabic: "مقاوم", example: "This fabric is water-resistant.", unit: "u3" },
  { word: "reveal", pos: "VERB", meaning: "To make something known or visible", arabic: "يكشف", example: "The report revealed new details about the case.", unit: "u3" },
  { word: "verify", pos: "VERB", meaning: "To confirm the truth or accuracy of something", arabic: "يتحقق من", example: "Please verify your email address.", unit: "u3" },
];

// Global state
let totalScore = 0, correctCount = 0, streak = 0, wordsSeenSet = new Set();

function updateScoreUI() {
  document.getElementById('totalScore').textContent = totalScore;
  document.getElementById('correctCount').textContent = correctCount;
  document.getElementById('streakCount').textContent = streak;
  document.getElementById('wordsLearned').textContent = wordsSeenSet.size;
}

function addScore(pts, correct, word) {
  if (correct) {
    totalScore += pts;
    correctCount++;
    streak++;
  } else {
    streak = 0;
  }
  if (word) wordsSeenSet.add(word);
  updateScoreUI();
}

// ============================================================
// NAVIGATION
// ============================================================
function showSection(id) {
  document.querySelectorAll('.section').forEach(s => s.classList.remove('active'));
  document.querySelectorAll('.nav-btn').forEach(b => b.classList.remove('active'));
  document.getElementById(id).classList.add('active');
  const btns = document.querySelectorAll('.nav-btn');
  btns.forEach(b => { if (b.getAttribute('onclick').includes(id)) b.classList.add('active'); });

  if (id === 'glossary') renderGlossary('all');
  if (id === 'flashcards') initFlashcards();
  if (id === 'mcq-en') generateMCQ_EN();
  if (id === 'mcq-ar') generateMCQ_AR();
  if (id === 'fill') generateFill();
  if (id === 'matching') generateMatching();
  if (id === 'truefalse') generateTF();
  if (id === 'scramble') generateScramble();
  if (id === 'wordsearch') generateWordSearch();
}

// ============================================================
// GLOSSARY
// ============================================================
function renderGlossary(filter) {
  const tbody = document.getElementById('glossaryBody');
  let filtered = words;
  if (filter === 'u1') filtered = words.filter(w => w.unit === 'u1');
  else if (filter === 'u3') filtered = words.filter(w => w.unit === 'u3');
  else if (['VERB','NOUN','ADJ'].includes(filter)) filtered = words.filter(w => w.pos.toUpperCase().includes(filter));

  tbody.innerHTML = filtered.map(w => `
    <tr style="cursor:pointer" onclick="this.nextElementSibling.style.display = this.nextElementSibling.style.display==='table-row'?'none':'table-row'">
      <td class="word-cell">${w.word}</td>
      <td><span class="pos-pill">${w.pos}</span></td>
      <td style="color:var(--muted); font-size:13px">${w.meaning}</td>
      <td class="ar-cell">${w.arabic}</td>
      <td><span class="unit-tag ${w.unit}">${w.unit === 'u1' ? 'U1' : 'U3'}</span></td>
    </tr>
    <tr style="display:none">
      <td colspan="5" style="padding:10px 14px; font-size:13px; color:var(--accent3); font-style:italic; background:rgba(0,212,170,0.04)">
        💬 ${w.example}
      </td>
    </tr>
  `).join('');
}

function filterGlossary(f, el) {
  document.querySelectorAll('.filter-chip').forEach(c => c.classList.remove('active'));
  el.classList.add('active');
  renderGlossary(f);
}

// ============================================================
// FLASHCARDS
// ============================================================
let fcIndex = 0, fcWords = [], fcFlipped = false;

function initFlashcards() {
  fcWords = shuffle([...words]);
  fcIndex = 0;
  renderFlashcard();
}

function renderFlashcard() {
  const w = fcWords[fcIndex];
  const el = document.getElementById('flashcard');
  el.classList.remove('flipped');
  fcFlipped = false;
  document.getElementById('fcWord').textContent = w.word;
  document.getElementById('fcPos').textContent = w.pos;
  document.getElementById('fcMeaning').textContent = w.meaning;
  document.getElementById('fcArabic').textContent = w.arabic;
  document.getElementById('fcExample').textContent = '💬 ' + w.example;
  document.getElementById('fcCounter').textContent = `${fcIndex+1} / ${fcWords.length}`;
  document.getElementById('fcProgress').style.width = ((fcIndex+1)/fcWords.length*100) + '%';
  wordsSeenSet.add(w.word);
  updateScoreUI();
}

function flipCard() {
  document.getElementById('flashcard').classList.toggle('flipped');
}

function nextCard() {
  fcIndex = (fcIndex + 1) % fcWords.length;
  renderFlashcard();
}

function prevCard() {
  fcIndex = (fcIndex - 1 + fcWords.length) % fcWords.length;
  renderFlashcard();
}

function shuffleCards() { fcWords = shuffle([...words]); fcIndex = 0; renderFlashcard(); }
function resetCards() { fcIndex = 0; renderFlashcard(); }

// ============================================================
// MCQ ENGLISH
// ============================================================
let mcqEnAnswered = 0, mcqEnTotal = 0, mcqEnCorrect = 0;

function generateMCQ_EN() {
  const pool = shuffle([...words]).slice(0, 10);
  mcqEnTotal = pool.length; mcqEnAnswered = 0; mcqEnCorrect = 0;
  updateStreakBar('streakBarEN', 10);
  const html = pool.map((w, i) => {
    const distractors = shuffle(words.filter(x => x.word !== w.word)).slice(0, 3).map(x => x.meaning);
    const opts = shuffle([w.meaning, ...distractors]);
    wordsSeenSet.add(w.word);
    return `
    <div class="question-card" id="mcqen-${i}">
      <div class="q-num">Question ${i+1} of ${pool.length} • Unit ${w.unit === 'u1' ? '1' : '3'}</div>
      <div class="q-text">What does <strong style="color:var(--accent)">${w.word}</strong> mean?</div>
      <div class="options">${opts.map((o, j) => `
        <button class="option" onclick="answerMCQ_EN(this, ${o === w.meaning}, ${i}, 'mcqen')" data-correct="${o === w.meaning}">
          <span class="opt-letter">${'ABCD'[j]}</span>${o}
        </button>`).join('')}
      </div>
      <div class="feedback" id="fb-mcqen-${i}"></div>
    </div>`;
  }).join('');
  document.getElementById('mcqEnContainer').innerHTML = html;
  document.getElementById('mcqEnProgress').style.width = '0%';
}

function answerMCQ_EN(btn, correct, qIdx, prefix) {
  const card = document.getElementById(`${prefix}-${qIdx}`);
  if (card.dataset.answered) return;
  card.dataset.answered = true;
  mcqEnAnswered++;
  const fb = document.getElementById(`fb-${prefix}-${qIdx}`);
  btn.classList.add(correct ? 'correct' : 'wrong');
  card.querySelectorAll('.option').forEach(b => {
    b.disabled = true;
    if (b.dataset.correct === 'true') b.classList.add('correct');
  });
  fb.className = `feedback show ${correct ? 'correct-fb' : 'wrong-fb'}`;
  fb.textContent = correct ? '✅ Correct! Great job!' : `❌ The correct answer is highlighted above.`;
  addScore(correct ? 10 : 0, correct, null);
  if (correct) mcqEnCorrect++;
  updateStreakBar('streakBarEN', 10, mcqEnAnswered, correct);
  document.getElementById('mcqEnProgress').style.width = (mcqEnAnswered/mcqEnTotal*100) + '%';
  if (mcqEnAnswered === mcqEnTotal) setTimeout(() => showResults(mcqEnCorrect, mcqEnTotal, 'EN MCQ', generateMCQ_EN), 800);
}

// ============================================================
// MCQ ARABIC
// ============================================================
let mcqArAnswered = 0, mcqArTotal = 0, mcqArCorrect = 0;

function generateMCQ_AR() {
  const pool = shuffle([...words]).slice(0, 10);
  mcqArTotal = pool.length; mcqArAnswered = 0; mcqArCorrect = 0;
  updateStreakBar('streakBarAR', 10);
  const html = pool.map((w, i) => {
    const distractors = shuffle(words.filter(x => x.word !== w.word)).slice(0, 3).map(x => x.arabic);
    const opts = shuffle([w.arabic, ...distractors]);
    wordsSeenSet.add(w.word);
    return `
    <div class="question-card" id="mcqar-${i}">
      <div class="q-num">سؤال ${i+1} من ${pool.length}</div>
      <div class="q-text q-ar">ما معنى كلمة <strong style="color:var(--accent)">${w.word}</strong>؟</div>
      <div class="options">${opts.map((o, j) => `
        <button class="option" onclick="answerMCQ_AR(this, ${o === w.arabic}, ${i})" data-correct="${o === w.arabic}" style="font-family:'Noto Naskh Arabic',serif;direction:rtl;font-size:16px">
          <span class="opt-letter">${'ABCD'[j]}</span>${o}
        </button>`).join('')}
      </div>
      <div class="feedback" id="fb-mcqar-${i}"></div>
    </div>`;
  }).join('');
  document.getElementById('mcqArContainer').innerHTML = html;
  document.getElementById('mcqArProgress').style.width = '0%';
}

function answerMCQ_AR(btn, correct, qIdx) {
  const card = document.getElementById(`mcqar-${qIdx}`);
  if (card.dataset.answered) return;
  card.dataset.answered = true;
  mcqArAnswered++;
  const fb = document.getElementById(`fb-mcqar-${qIdx}`);
  btn.classList.add(correct ? 'correct' : 'wrong');
  card.querySelectorAll('.option').forEach(b => { b.disabled = true; if (b.dataset.correct === 'true') b.classList.add('correct'); });
  fb.className = `feedback show ${correct ? 'correct-fb' : 'wrong-fb'}`;
  fb.textContent = correct ? '✅ ممتاز! إجابة صحيحة!' : `❌ الإجابة الصحيحة محددة أعلاه.`;
  addScore(correct ? 10 : 0, correct, null);
  if (correct) mcqArCorrect++;
  updateStreakBar('streakBarAR', 10, mcqArAnswered, correct);
  document.getElementById('mcqArProgress').style.width = (mcqArAnswered/mcqArTotal*100) + '%';
  if (mcqArAnswered === mcqArTotal) setTimeout(() => showResults(mcqArCorrect, mcqArTotal, 'Arabic MCQ', generateMCQ_AR), 800);
}

// ============================================================
// FILL IN THE BLANK
// ============================================================
let fillWords = [];

function generateFill() {
  fillWords = shuffle([...words]).slice(0, 8);
  const html = fillWords.map((w, i) => {
    const sentence = w.example.replace(new RegExp(w.word, 'i'), `<input class="fill-input" id="fill-${i}" placeholder="..." autocomplete="off" spellcheck="false">`);
    wordsSeenSet.add(w.word);
    return `
    <div class="question-card" id="fill-card-${i}">
      <div class="q-num">Question ${i+1} • ${w.unit === 'u1' ? 'Unit 1' : 'Unit 3'}</div>
      <div style="font-size:13px;color:var(--muted);margin-bottom:8px">Complete the sentence: (Arabic hint: <span style="font-family:'Noto Naskh Arabic',serif;color:var(--gold)">${w.arabic}</span>)</div>
      <div class="q-text" style="line-height:2">${sentence}</div>
      <div class="feedback" id="fill-fb-${i}"></div>
    </div>`;
  }).join('');
  document.getElementById('fillContainer').innerHTML = html;
  document.getElementById('fillProgress').style.width = '0%';
}

function checkAllFill() {
  let correct = 0;
  fillWords.forEach((w, i) => {
    const inp = document.getElementById(`fill-${i}`);
    if (!inp) return;
    const val = inp.value.trim().toLowerCase();
    const answer = w.word.toLowerCase();
    const fb = document.getElementById(`fill-fb-${i}`);
    const isCorrect = val === answer;
    inp.classList.remove('correct','wrong');
    inp.classList.add(isCorrect ? 'correct' : 'wrong');
    inp.disabled = true;
    fb.className = `feedback show ${isCorrect ? 'correct-fb' : 'wrong-fb'}`;
    fb.textContent = isCorrect ? `✅ Correct!` : `❌ Answer: "${w.word}"`;
    if (isCorrect) { correct++; addScore(10, true, w.word); } else { addScore(0, false, w.word); }
  });
  document.getElementById('fillProgress').style.width = '100%';
  setTimeout(() => showResults(correct, fillWords.length, 'Fill in the Blank', generateFill), 600);
}

// ============================================================
// MATCHING
// ============================================================
let matchPool = [], matchSelected = null, matchMatched = 0;

function generateMatching() {
  matchPool = shuffle([...words]).slice(0, 8);
  matchSelected = null; matchMatched = 0;
  const leftWords = shuffle([...matchPool]);
  const rightWords = shuffle([...matchPool]);
  const leftHTML = leftWords.map((w, i) => `<div class="match-item" id="ml-${w.word}" onclick="selectMatch('left','${w.word}')">${w.word}</div>`).join('');
  const rightHTML = rightWords.map((w, i) => `<div class="match-item match-ar" id="mr-${w.word}" onclick="selectMatch('right','${w.word}')">${w.arabic}</div>`).join('');
  document.getElementById('matchContainer').innerHTML = `
    <div>
      <div class="match-col-title">🇬🇧 Words</div>
      ${leftHTML}
    </div>
    <div>
      <div class="match-col-title">🇸🇦 المعاني</div>
      ${rightHTML}
    </div>`;
  document.getElementById('matchProgress').style.width = '0%';
  matchPool.forEach(w => wordsSeenSet.add(w.word));
  updateScoreUI();
}

function selectMatch(side, word) {
  const el = document.getElementById(side === 'left' ? `ml-${word}` : `mr-${word}`);
  if (el.classList.contains('matched')) return;

  if (!matchSelected) {
    document.querySelectorAll('.match-item.selected-match').forEach(e => e.classList.remove('selected-match'));
    el.classList.add('selected-match');
    matchSelected = { side, word };
  } else {
    if (matchSelected.side === side) {
      document.querySelectorAll('.match-item.selected-match').forEach(e => e.classList.remove('selected-match'));
      el.classList.add('selected-match');
      matchSelected = { side, word };
      return;
    }
    const leftWord = matchSelected.side === 'left' ? matchSelected.word : word;
    const rightWord = matchSelected.side === 'right' ? matchSelected.word : word;
    const correct = leftWord === rightWord;
    const ml = document.getElementById(`ml-${leftWord}`);
    const mr = document.getElementById(`mr-${rightWord}`);
    if (correct) {
      ml.classList.remove('selected-match'); mr.classList.remove('selected-match');
      ml.classList.add('matched'); mr.classList.add('matched');
      matchMatched++;
      addScore(15, true, leftWord);
      document.getElementById('matchProgress').style.width = (matchMatched/matchPool.length*100) + '%';
      if (matchMatched === matchPool.length) setTimeout(() => showResults(matchPool.length, matchPool.length, 'Matching', generateMatching), 600);
    } else {
      ml.classList.add('wrong-match'); mr.classList.add('wrong-match');
      addScore(0, false, null);
      setTimeout(() => { ml.classList.remove('wrong-match','selected-match'); mr.classList.remove('wrong-match','selected-match'); }, 600);
    }
    matchSelected = null;
  }
}

// ============================================================
// TRUE / FALSE
// ============================================================
let tfAnswered = 0, tfTotal = 0, tfCorrect = 0, tfPool = [];

function generateTF() {
  tfPool = shuffle([...words]).slice(0, 10);
  tfTotal = tfPool.length; tfAnswered = 0; tfCorrect = 0;
  updateStreakBar('streakBarTF', 10);
  const html = tfPool.map((w, i) => {
    const useFake = Math.random() > 0.5;
    let shownMeaning = w.meaning;
    let isTrue = true;
    if (useFake) {
      const other = words.filter(x => x.word !== w.word);
      shownMeaning = other[Math.floor(Math.random()*other.length)].meaning;
      isTrue = false;
    }
    wordsSeenSet.add(w.word);
    return `
    <div class="tf-card" id="tf-${i}">
      <div class="q-num">Statement ${i+1} of ${tfPool.length}</div>
      <div class="tf-statement">
        The word <strong style="color:var(--accent)">${w.word}</strong> means:
        <span style="color:var(--text)"> "${shownMeaning}"</span>
      </div>
      <div class="tf-buttons">
        <button class="tf-btn true-btn" onclick="answerTF(this, ${isTrue}, ${i})">✅ True</button>
        <button class="tf-btn false-btn" onclick="answerTF(this, ${!isTrue}, ${i})">❌ False</button>
      </div>
      <div class="feedback" id="tf-fb-${i}"></div>
    </div>`;
  }).join('');
  document.getElementById('tfContainer').innerHTML = html;
  document.getElementById('tfProgress').style.width = '0%';
}

function answerTF(btn, correct, qIdx) {
  const card = document.getElementById(`tf-${qIdx}`);
  if (card.dataset.answered) return;
  card.dataset.answered = true;
  tfAnswered++;
  btn.classList.add(correct ? 'selected-correct' : 'selected-wrong');
  card.querySelectorAll('.tf-btn').forEach(b => b.disabled = true);
  const fb = document.getElementById(`tf-fb-${qIdx}`);
  fb.className = `feedback show ${correct ? 'correct-fb' : 'wrong-fb'}`;
  fb.textContent = correct ? '✅ Correct!' : `❌ Wrong! Check the actual meaning.`;
  addScore(correct ? 10 : 0, correct, null);
  if (correct) tfCorrect++;
  updateStreakBar('streakBarTF', 10, tfAnswered, correct);
  document.getElementById('tfProgress').style.width = (tfAnswered/tfTotal*100) + '%';
  if (tfAnswered === tfTotal) setTimeout(() => showResults(tfCorrect, tfTotal, 'True/False', generateTF), 800);
}

// ============================================================
// SCRAMBLE
// ============================================================
let scramblePool = [], scrambleIdx = 0, scrambleAnswer = [], scrambleLetterMap = [];

function generateScramble() {
  scramblePool = shuffle(words.filter(w => w.word.length <= 12 && !w.word.includes(' '))).slice(0, 8);
  scrambleIdx = 0;
  renderScramble();
}

function renderScramble() {
  if (scrambleIdx >= scramblePool.length) { showResults(scramblePool.length, scramblePool.length, 'Scramble', generateScramble); return; }
  const w = scramblePool[scrambleIdx];
  scrambleAnswer = [];
  const letters = shuffle(w.word.toUpperCase().split(''));
  scrambleLetterMap = letters.map((l, i) => ({ letter: l, id: i, used: false }));
  wordsSeenSet.add(w.word);
  updateScoreUI();
  document.getElementById('scrambleContainer').innerHTML = `
    <div class="scramble-box">
      <div class="q-num">Word ${scrambleIdx+1} of ${scramblePool.length}</div>
      <div style="font-size:14px;color:var(--muted);margin-bottom:4px">${w.meaning}</div>
      <div style="font-family:'Noto Naskh Arabic',serif;font-size:18px;color:var(--gold);direction:rtl;margin-bottom:12px">${w.arabic}</div>
      <div class="answer-tiles" id="answerTiles"></div>
      <div class="scrambled-word" id="scrambleTiles">
        ${scrambleLetterMap.map(item => `<div class="letter-tile" id="stile-${item.id}" onclick="pickLetter(${item.id})">${item.letter}</div>`).join('')}
      </div>
      <div class="feedback" id="scramble-fb"></div>
    </div>`;
  document.getElementById('scrambleProgress').style.width = ((scrambleIdx+1)/scramblePool.length*100) + '%';
}

function pickLetter(id) {
  const item = scrambleLetterMap[id];
  if (item.used) return;
  item.used = true;
  scrambleAnswer.push({ letter: item.letter, srcId: id });
  document.getElementById(`stile-${id}`).classList.add('used');
  renderAnswerTiles();
}

function renderAnswerTiles() {
  const container = document.getElementById('answerTiles');
  container.innerHTML = scrambleAnswer.map((item, i) => `
    <div class="answer-tile" onclick="removeLetter(${i})">${item.letter}</div>`).join('');
}

function removeLetter(i) {
  const item = scrambleAnswer.splice(i, 1)[0];
  document.getElementById(`stile-${item.srcId}`).classList.remove('used');
  scrambleLetterMap[item.srcId].used = false;
  renderAnswerTiles();
}

function checkScramble() {
  const w = scramblePool[scrambleIdx];
  const answer = scrambleAnswer.map(a => a.letter).join('').toLowerCase();
  const correct = answer === w.word.toLowerCase();
  const fb = document.getElementById('scramble-fb');
  fb.className = `feedback show ${correct ? 'correct-fb' : 'wrong-fb'}`;
  fb.textContent = correct ? `✅ Correct! "${w.word}"` : `❌ Correct answer: "${w.word.toUpperCase()}"`;
  addScore(correct ? 15 : 0, correct, w.word);
  setTimeout(nextScramble, 1200);
}

function nextScramble() {
  scrambleIdx++;
  renderScramble();
}

// ============================================================
// WORD SEARCH
// ============================================================
let wsGrid = [], wsSize = 12, wsFoundWords = [], wsTargetWords = [], wsCells = [];
let wsSelecting = false, wsStart = null, wsSelected = [];

function generateWordSearch() {
  const pool = shuffle(words.filter(w => !w.word.includes(' ') && w.word.length <= wsSize)).slice(0, 8);
  wsTargetWords = pool.map(w => w.word.toUpperCase());
  wsFoundWords = [];
  wsGrid = Array.from({length: wsSize}, () => Array(wsSize).fill(''));

  wsTargetWords.forEach(word => {
    let placed = false, tries = 0;
    while (!placed && tries < 100) {
      tries++;
      const dir = Math.random() > 0.5 ? 'h' : 'v';
      const r = Math.floor(Math.random() * (dir === 'h' ? wsSize : wsSize - word.length));
      const c = Math.floor(Math.random() * (dir === 'v' ? wsSize : wsSize - word.length));
      let fits = true;
      for (let k = 0; k < word.length; k++) {
        const row = dir === 'h' ? r : r + k;
        const col = dir === 'h' ? c + k : c;
        if (wsGrid[row][col] && wsGrid[row][col] !== word[k]) { fits = false; break; }
      }
      if (fits) {
        for (let k = 0; k < word.length; k++) {
          const row = dir === 'h' ? r : r + k;
          const col = dir === 'h' ? c + k : c;
          wsGrid[row][col] = word[k];
        }
        placed = true;
      }
    }
  });

  const alpha = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ';
  for (let r = 0; r < wsSize; r++)
    for (let c = 0; c < wsSize; c++)
      if (!wsGrid[r][c]) wsGrid[r][c] = alpha[Math.floor(Math.random()*26)];

  document.getElementById('wsContainer').innerHTML = `
    <div style="overflow-x:auto;margin-bottom:16px">
      <div class="ws-grid" id="wsGrid" style="grid-template-columns:repeat(${wsSize},36px)"></div>
    </div>
    <div style="font-size:12px;color:var(--muted);margin-bottom:8px">Find these words:</div>
    <div class="ws-words">${wsTargetWords.map(w => `<span class="ws-word" id="wsw-${w}">${w}</span>`).join('')}</div>
    <div style="margin-top:12px;font-size:13px;color:var(--muted)">Click first letter, then last letter of the word</div>`;

  const gridEl = document.getElementById('wsGrid');
  wsCells = [];
  wsGrid.forEach((row, r) => {
    row.forEach((ch, c) => {
      const cell = document.createElement('div');
      cell.className = 'ws-cell';
      cell.textContent = ch;
      cell.dataset.r = r;
      cell.dataset.c = c;
      cell.onclick = () => wsClickCell(r, c);
      gridEl.appendChild(cell);
      wsCells.push(cell);
    });
  });
}

function wsClickCell(r, c) {
  if (!wsStart) {
    wsStart = {r, c};
    getCell(r, c).classList.add('selected');
  } else {
    // Try to make a word from start to end
    const cells = getCellsBetween(wsStart.r, wsStart.c, r, c);
    if (cells) {
      const word = cells.map(([cr, cc]) => wsGrid[cr][cc]).join('');
      const rev = word.split('').reverse().join('');
      const found = wsTargetWords.find(w => w === word || w === rev);
      if (found && !wsFoundWords.includes(found)) {
        wsFoundWords.push(found);
        cells.forEach(([cr, cc]) => { getCell(cr, cc).classList.remove('selected'); getCell(cr, cc).classList.add('found'); });
        document.getElementById(`wsw-${found}`).classList.add('found');
        addScore(20, true, found.toLowerCase());
        if (wsFoundWords.length === wsTargetWords.length) setTimeout(() => showResults(wsTargetWords.length, wsTargetWords.length, 'Word Search', generateWordSearch), 600);
      } else {
        cells.forEach(([cr, cc]) => getCell(cr, cc).classList.remove('selected'));
        getCell(wsStart.r, wsStart.c).classList.remove('selected');
      }
    } else {
      getCell(wsStart.r, wsStart.c).classList.remove('selected');
    }
    wsStart = null;
  }
}

function getCell(r, c) {
  return wsCells[r * wsSize + c];
}

function getCellsBetween(r1, c1, r2, c2) {
  if (r1 === r2) {
    const cells = [];
    const step = c2 >= c1 ? 1 : -1;
    for (let c = c1; c !== c2 + step; c += step) cells.push([r1, c]);
    return cells;
  }
  if (c1 === c2) {
    const cells = [];
    const step = r2 >= r1 ? 1 : -1;
    for (let r = r1; r !== r2 + step; r += step) cells.push([r, c1]);
    return cells;
  }
  return null;
}

// ============================================================
// HELPERS
// ============================================================
function shuffle(arr) {
  for (let i = arr.length - 1; i > 0; i--) {
    const j = Math.floor(Math.random() * (i + 1));
    [arr[i], arr[j]] = [arr[j], arr[i]];
  }
  return arr;
}

function updateStreakBar(id, total, answered, correct) {
  const bar = document.getElementById(id);
  if (!bar) return;
  if (!answered) {
    bar.innerHTML = Array(total).fill(`<div class="streak-dot"></div>`).join('');
    return;
  }
  const dots = bar.querySelectorAll('.streak-dot');
  if (dots[answered - 1]) dots[answered - 1].classList.add(correct ? 'correct' : 'wrong');
}

function showResults(correct, total, exerciseName, nextFn) {
  const pct = Math.round(correct / total * 100);
  let emoji = pct >= 90 ? '🏆' : pct >= 70 ? '🎉' : pct >= 50 ? '😊' : '💪';
  let title = pct >= 90 ? 'Outstanding!' : pct >= 70 ? 'Great Job!' : pct >= 50 ? 'Good effort!' : 'Keep Practicing!';
  let msg = `You got ${correct} out of ${total} correct (${pct}%) in ${exerciseName}.`;

  document.getElementById('resultEmoji').textContent = emoji;
  document.getElementById('resultScore').textContent = `${correct}/${total}`;
  document.getElementById('resultTitle').textContent = title;
  document.getElementById('resultMsg').textContent = msg;
  document.getElementById('resultNext').onclick = () => { closeResults(); if (nextFn) nextFn(); };
  document.getElementById('resultsOverlay').classList.add('show');
}

function closeResults() {
  document.getElementById('resultsOverlay').classList.remove('show');
}

// Init
renderGlossary('all');
updateScoreUI();
</script>
</body>
</html>
